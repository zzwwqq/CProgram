//用数组处理斐波那契数列问题,只计算20个数.
#include<stdio.h>
int main()
{int i;
int f[20]={1,1};//特别注意：下标是从0开始，即0-19.不存在数组元素f[20].这一行是对最前面的两项f[0],f[1],赋初值1
for(i=2;i<=19;i++)//或者i<20
   f[i]=f[i-2]+f[i-1];//先后求出f[2]~f[19]
for(i=0;i<=19;i++)
	
{	if(i%5==0)//每五个数换一行
printf("\n");
printf("%12d",f[i]);//输出这20个数
}
printf("\n");
return 0;
}
